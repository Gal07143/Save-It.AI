apiVersion: v1
kind: Service
metadata:
  name: saveit-frontend
  namespace: saveit
  labels:
    app.kubernetes.io/name: saveit-frontend
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: saveit-frontend
